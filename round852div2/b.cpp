#include <bits/stdc++.h>

typedef long long ll;

void	solve()
{
	ll sum_local_max, sum_local_min;	

	scanf(" %lld %lld", &sum_local_max, &sum_local_min);

}

int	main()
{
	int	t;

	scanf("%d", &t);
	for (int i = 0; i < t; i++)
	{
		solve();
	}
	return (0);
}
